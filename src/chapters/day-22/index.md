시작:
┌──────────────────────────────┐
│ sumSalaries(company)         │
│ sum = 0                      │
└──────────────────────────────┘

sales 처리:
┌──────────────────────────────┐
│ sumSalaries(sales)           │ ← 실행 중
│ reduce → 2600                │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 0                      │
└──────────────────────────────┘

sales 완료:
┌──────────────────────────────┐
│ sumSalaries(company)         │ ← 다시 실행
│ sum = 2600                   │
└──────────────────────────────┘

development 처리:
┌──────────────────────────────┐
│ sumSalaries(development)     │ ← 실행 중
│ sum = 0                      │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 2600                   │
└──────────────────────────────┘

sites 처리:
┌──────────────────────────────┐
│ sumSalaries(sites)           │ ← 실행 중
│ reduce → 3800                │
├──────────────────────────────┤
│ sumSalaries(development)     │ ← 대기 중
│ sum = 0                      │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 2600                   │
└──────────────────────────────┘

sites 완료:
┌──────────────────────────────┐
│ sumSalaries(development)     │ ← 다시 실행
│ sum = 3800                   │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 2600                   │
└──────────────────────────────┘

internals 처리:
┌──────────────────────────────┐
│ sumSalaries(internals)       │ ← 실행 중
│ reduce → 1300                │
├──────────────────────────────┤
│ sumSalaries(development)     │ ← 대기 중
│ sum = 3800                   │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 2600                   │
└──────────────────────────────┘

internals 완료:
┌──────────────────────────────┐
│ sumSalaries(development)     │ ← 다시 실행
│ sum = 5100                   │
├──────────────────────────────┤
│ sumSalaries(company)         │ ← 대기 중
│ sum = 2600                   │
└──────────────────────────────┘

development 완료:
┌──────────────────────────────┐
│ sumSalaries(company)         │ ← 다시 실행
│ sum = 7700                   │
└──────────────────────────────┘

최종 반환: 7700